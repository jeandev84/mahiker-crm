{% extends 'base.html.twig' %}

{% block title %}{{ event.title }}{% endblock %}

{% block body %}

    <h1 class="mt-3 mb-3">{{ event.title }}</h1>

    {{ include('admin/events/_form.html.twig', {'button_label': 'Обновить'}) }}

{% endblock %}
